# frozen_string_literal: true

RSpec.describe Xcpretty::Sonarqube::Compiler::Formatter do
  it "has a version number" do
    expect(Xcpretty::Sonarqube::Compiler::Formatter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(false)
  end
end
